# 🏗️ Arquitectura del Sistema de Notificaciones

```
┌─────────────────────────────────────────────────────────────────────┐
│                     SISTEMA DE NOTIFICACIONES                       │
│                         (100% GRATUITO)                             │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│   📱 Cliente    │
│   (Frontend)    │
└────────┬────────┘
         │ HTTP Request
         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     🚀 API Node.js + Express                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                    📡 Routes (API)                          │  │
│  ├─────────────────────────────────────────────────────────────┤  │
│  │  • POST /api/subscriptions          [Crear suscripción]    │  │
│  │  • POST /api/subscriptions/:id/renew [Renovar]             │  │
│  │  • POST /api/scheduler/run           [Verificar todo]      │  │
│  │  • GET  /api/health                  [Estado]              │  │
│  └──────────────────────┬──────────────────────────────────────┘  │
│                         │                                          │
│                         ▼                                          │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                 🎮 Controllers                              │  │
│  ├─────────────────────────────────────────────────────────────┤  │
│  │  • subscription.controller.js                               │  │
│  │  • Lógica de negocio                                        │  │
│  └──────────────────────┬──────────────────────────────────────┘  │
│                         │                                          │
│                         ▼                                          │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                 ⚙️ Services                                 │  │
│  ├─────────────────────────────────────────────────────────────┤  │
│  │                                                             │  │
│  │  ┌────────────────────────────────────────────────────┐    │  │
│  │  │  notification.service.js                           │    │  │
│  │  │  • Envía notificaciones                            │    │  │
│  │  │  • Registra en Firebase                            │    │  │
│  │  └─────────────────┬──────────────────────────────────┘    │  │
│  │                    │                                        │  │
│  │                    ▼                                        │  │
│  │  ┌────────────────────────────────────────────────────┐    │  │
│  │  │  email.service.js                                  │    │  │
│  │  │  • Templates HTML                                  │    │  │
│  │  │  • Envío de emails con Nodemailer                  │    │  │
│  │  └─────────────────┬──────────────────────────────────┘    │  │
│  │                    │                                        │  │
│  │  ┌────────────────────────────────────────────────────┐    │  │
│  │  │  scheduler.service.js                              │    │  │
│  │  │  • Verifica suscripciones próximas a vencer        │    │  │
│  │  │  • Marca suscripciones expiradas                   │    │  │
│  │  └────────────────────────────────────────────────────┘    │  │
│  │                                                             │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
         │                                        │
         │ Guardar/Leer                          │ Enviar Email
         ▼                                        ▼
┌──────────────────────┐              ┌──────────────────────┐
│  🔥 Firebase         │              │  📧 Gmail SMTP       │
│  Firestore           │              │  (Nodemailer)        │
├──────────────────────┤              ├──────────────────────┤
│ Colecciones:         │              │ SMTP_HOST: gmail.com │
│ • users              │              │ SMTP_PORT: 587       │
│ • subscriptions      │              │ Límite: 500/día      │
│ • plans              │              │ Costo: GRATIS        │
│ • notifications      │              └──────────────────────┘
│                      │
│ Límite: 1GB gratis   │
│ Costo: GRATIS        │
└──────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    ⏰ AUTOMATIZACIÓN                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  🌐 Servicio Externo de Cron (cron-job.org)               │   │
│  │                                                            │   │
│  │  Horario: 0 9 * * * (Diario a las 9 AM)                   │   │
│  │  ↓                                                         │   │
│  │  POST https://tu-api.com/api/scheduler/run                │   │
│  │  ↓                                                         │   │
│  │  • Verifica todas las suscripciones activas               │   │
│  │  • Envía notificaciones si vencen en 7, 3 o 1 día         │   │
│  │  • Marca expiradas                                         │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  Costo: GRATIS                                                      │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    📧 FLUJO DE NOTIFICACIONES                       │
└─────────────────────────────────────────────────────────────────────┘

1️⃣ SUSCRIPCIÓN RECIBIDA
   Usuario crea suscripción
   → API recibe request
   → Guarda en Firestore
   → notification.service.sendSubscriptionReceivedNotification()
   → email.service.sendSubscriptionReceivedEmail()
   → Gmail envía email
   → Registra en colección 'notifications'

2️⃣ PLAN PRÓXIMO A VENCER (Verificación Diaria)
   Cron job ejecuta /api/scheduler/run
   → scheduler.service.checkExpiringSubscriptions()
   → Lee todas las suscripciones activas de Firestore
   → Filtra las que vencen en 7, 3 o 1 día
   → Para cada una:
      → Verifica que no se haya enviado notificación hoy
      → notification.service.sendPlanExpirationNotification()
      → email.service.sendPlanExpiringEmail()
      → Gmail envía email
      → Actualiza lastExpirationNotification en Firestore
      → Registra en colección 'notifications'

3️⃣ PLAN RENOVADO
   Usuario renueva suscripción
   → API recibe request
   → Actualiza expirationDate en Firestore
   → notification.service.sendPlanRenewalNotification()
   → email.service.sendPlanRenewedEmail()
   → Gmail envía email
   → Registra en colección 'notifications'

┌─────────────────────────────────────────────────────────────────────┐
│                    💰 COSTOS: $0/mes                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ✅ Firebase Firestore    : $0 (1GB + 50K lecturas/día)            │
│  ✅ Gmail/Nodemailer      : $0 (500 emails/día)                    │
│  ✅ cron-job.org          : $0 (ilimitado)                         │
│  ✅ Hosting (Render free) : $0 (opcional)                          │
│                             ─────────────────                       │
│                    TOTAL   : $0/mes                                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    📊 ESTRUCTURA DE DATOS                           │
└─────────────────────────────────────────────────────────────────────┘

users/
  └─ {userId}
     ├─ name: string
     ├─ email: string
     ├─ phone: string
     ├─ createdAt: timestamp
     └─ status: "active" | "inactive"

subscriptions/
  └─ {subscriptionId}
     ├─ userId: string
     ├─ planId: string
     ├─ planName: string
     ├─ status: "active" | "expired" | "cancelled"
     ├─ startDate: timestamp
     ├─ expirationDate: timestamp
     ├─ price: number
     ├─ currency: string
     ├─ autoRenew: boolean
     ├─ lastExpirationNotification: timestamp (opcional)
     ├─ createdAt: timestamp
     └─ updatedAt: timestamp

plans/
  └─ {planId}
     ├─ name: string
     ├─ description: string
     ├─ price: number
     ├─ currency: string
     ├─ duration: number
     ├─ features: string[]
     └─ active: boolean

notifications/
  └─ {notificationId}
     ├─ type: "SUBSCRIPTION_RECEIVED" | "PLAN_EXPIRING" | "PLAN_RENEWED"
     ├─ userId: string
     ├─ email: string
     ├─ subscriptionId: string (opcional)
     ├─ sentAt: timestamp
     ├─ status: "sent" | "failed"
     └─ error: string (opcional)
